{
  "name": "moduleforge-monorepo",
  "version": "1.0.0",
  "description": "ModuleForge - Reusable Software Module Platform",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "build:vercel": "npm --prefix client run build",
    "start": "npm run start --prefix server",
    "db:push": "npm run prisma:push --prefix server",
    "db:generate": "npm run prisma:generate --prefix server",
    "postinstall": "npm run prisma:generate --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.3"
  }
}
